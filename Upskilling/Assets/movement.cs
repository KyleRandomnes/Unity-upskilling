using UnityEngine;

public class movement : MonoBehaviour
{
    public Rigidbody rb;

    void Start()
    {
        rb.useGravity = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
